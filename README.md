# My First Tmuxifier Dev Env

Run 

- `tmux`
- then `tmuxifier load-window log-app.window.sh` to start the programing env.

If you detach from the tmux `Ctrl-B D` then you can re-attach by `tmux a`.

